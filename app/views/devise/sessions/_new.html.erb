<%= simple_form_for(resource, as: resource_name, url: session_path(resource_name), class: 'form-signin') do |f| %>
  <div class="form-group row">
    <label class="col-md-4">Correo electrónico</label>
    <div class="col-md-8">
      <%= f.input :email, input_html: { class: 'form-control', style: 'margin-bottom:5px' }, label: false,
        placeholder: 'Correo electrónico', required: false, autofocus: true %>
    </div>
  </div>
  <div class="form-group row">
    <label class="col-md-4">Contraseña</label>
    <div class="col-md-8">
      <%= f.input :password, input_html: { class: 'form-control', style: 'margin-bottom:5px', maxlength: 9 },
        label: false, placeholder: 'Contraseña', required: false %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.button :submit, t(".sign_in"), class: 'btn-main pull-right' %>
  </div>
<% end %>
<%= render "devise/shared/links" %>
